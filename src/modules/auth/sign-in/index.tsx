import { Card, Form } from 'antd'

import { StyledTitle } from '../styles'

export const SignIn = () => {
  return (
    <Card>
      <StyledTitle>Sign Up</StyledTitle>
      <Form></Form>
    </Card>
  )
}
